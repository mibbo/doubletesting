{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "coveralls": "node --experimental-vm-modules node_modules/.bin/jest --coverage && cat ./coverage/lcov.info | node --experimental-vm-modules ./node_modules/coveralls/.bin/coveralls && rm -rf coverage/"
  },
  "jest": {
    "collectCoverage": true,
    "coverageReporters": [
      "lcov",
      "text"
    ]
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chai": "^4.2.0"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "jest": "^26.6.3",
    "mocha": "^8.2.1"
  }
}
